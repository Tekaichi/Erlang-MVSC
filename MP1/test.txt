
{ok,buffer} = c(buffer).

{ok,monitor} = c(monitor),
monitor:init().

{ok,producer} = c(producer),
producer:start(8).
{ok,consumer} =c(consumer),
consumer:start(8).
bounded_buffer ! read.
bounded_buffer ! read.

